/** @type {import('next').NextConfig} */
const nextConfig = {};
images: {
    remotepatterns: [
        {
            protocol: 'https',
            hostname: 'res.cloudinary.com',
            port: ''
        }
    ]
};
export default nextConfig;
